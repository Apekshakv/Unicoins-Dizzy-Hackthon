<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - UniCoins</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-image: url('images/backgro.jpg'); /* Replace with your image */
            background-size: cover;
            background-position: center;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 80px; /* Even more padding */
            border-radius: 25px; /* Larger border radius */
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3); /* Stronger shadow */
            width: 80vw; /* Use viewport width */
            max-width: 800px; /* Maximum width */
            text-align: center;
        }

        h2 {
            color: #D4AC0D;
            margin-bottom: 50px;
            font-size: 4em; /* Larger font size */
            font-weight: bold;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input[type="email"] {
            padding: 25px 35px;
            border: 1px solid #ddd;
            border-radius: 12px;
            margin-bottom: 40px;
            width: 90%; /* Larger input width */
            box-sizing: border-box;
            font-size: 1.6em;
        }

        button {
            background-color: #D4AC0D;
            color: white;
            padding: 28px 50px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 1.8em;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #B7950B;
        }

        p {
            margin-top: 40px;
            font-size: 1.6em;
        }

        a {
            color: #D4AC0D;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Login</h2>
        <form action="/login" method="POST">
            <input type="email" name="email" placeholder="Enter your email" required>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/register">Register</a></p>
        <a href="/">Back Home</a>
    </div>
</body>
</html>